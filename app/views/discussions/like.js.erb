str = '<span id="discussion-<%= @discussion.id %>-like"><%= link_to unlike_discussion_path(@discussion), method: :post, remote: true do  %><i class="fa fa-heart" aria-hidden="true"></i>(<%= @discussion.reload.likes_count %>)<% end %></span>'
$("#discussion-<%= @discussion.id %>-like").replaceWith(str)
